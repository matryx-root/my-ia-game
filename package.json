{
  "name": "my-ia-game",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": ">=22.13.1 <=22.17.1",
    "npm": ">=11.4.2"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node prisma/seed.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "heroku-postbuild": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && npm run prisma:generate && npm run prisma:migrate",
    "build": "cd frontend && npm run build",
    "test": "jest",
    "lint": "eslint .",
    "update-deps": "npm install npm@latest -g && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "Aplicación de juegos de IA educativos",
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "pg": "^8.16.0",
    "prisma": "^6.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "eslint": "^9.3.0",
    "jest": "^29.7.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "volta": {
    "node": "22.17.1",
    "npm": "11.4.2"
  }
}